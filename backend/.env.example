# Nebula Backend (.env.example)
# Copy to .env and fill in your Hypixel key (and future secrets like STRIPE_SECRET_KEY)

PORT=3001
HYPIXEL_KEY=your_hypixel_key_here
# Optional: override cache TTL (ms). Default 300000 (5 minutes)
CACHE_TTL_MS=300000
