# Nebula

Nebula is a lightweight Bedwars stats overlay for Hypixel.

## Dev
- Install: `npm i`
- Start: `npm run start` (build + run)
- Dev (main only): `npm run dev`

## Env
Copy `.env.example` to `.env` and set `HYPIXEL_KEY`.

## Status
WIP: name → UUID → player stats (stars, FKDR, WLR).
