# Example environment file for Nebula (desktop client)# Hypixel API Key

# Copy to .env and fill in per-user values. DO NOT COMMIT .env.# Get your API key by running /api new on Hypixel

HYPIXEL_KEY=your_hypixel_api_key_here

# Hypixel API Key (each user should generate their own via /api new)

HYPIXEL_KEY=your_hypixel_api_key_here# Discord OAuth2 (for Profile & Cloud Sync features)

# Create a Discord application at: https://discord.com/developers/applications

# Discord OAuth (Public Client - no secret required)# 

DISCORD_CLIENT_ID=your_discord_client_id_here# QUICK START (Public Client - NO SECRET NEEDED):

# DISCORD_CLIENT_SECRET=  # (Not needed for public desktop client with PKCE)# 1. Create a new application

# 2. Go to OAuth2 → General

# Firebase Config (public web config; security via Firestore/Storage Rules)# 3. Copy your CLIENT ID (paste below)

FIREBASE_API_KEY=your_firebase_api_key_here# 4. ENABLE "Public Client" toggle (IMPORTANT!)

FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com# 5. Add redirect URL: http://localhost:3000/auth/discord/callback

FIREBASE_PROJECT_ID=your_project_id# 

FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app# That's it! Client Secret is OPTIONAL for desktop apps.

FIREBASE_MESSAGING_SENDER_ID=your_sender_idDISCORD_CLIENT_ID=your_discord_client_id_here

FIREBASE_APP_ID=your_app_id

# Client Secret (OPTIONAL - only needed if "Public Client" is disabled)

# Backend API (used for secure Plus verification; hosts Stripe & server-side logic)# To get it: Click "Reset Secret" button → Copy immediately (shows only once!)

BACKEND_API_URL=https://your-backend.example.com# DISCORD_CLIENT_SECRET=your_discord_client_secret_here



# Development flags# Development Settings

NEBULA_DEVTOOLS=0# Set to '1' to open DevTools on launch

NODE_ENV=productionNEBULA_DEVTOOLS=0



# IMPORTANT: Stripe secrets (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET) must live ONLY on the backend.# Node Environment

# Do not place them here; they will be exposed in the packaged app.NODE_ENV=production

