# Nebula – Example .env
# Copy this file to ".env" during development.
# For the packaged EXE, you may also place a ".env" in one of these locations:
#  - %APPDATA%/Nebula/.env (recommended)
#  - next to the executable (Nebula.exe/.env)
#  - resources folder (nebula.config.json is also supported)
# Variables in the OS environment take precedence over this file.

########################
# Hypixel API
########################
# Generate your key with "/api new" on Hypixel.
HYPIXEL_KEY=

########################
# Discord OAuth2 (for login/cloud sync)
########################
# Create an application: https://discord.com/developers/applications
# Enable "Public Client" under OAuth2 if you don't want to ship a secret (PKCE flow).
DISCORD_CLIENT_ID=
# Optional; only needed if you DISABLE "Public Client" in Discord settings
# DISCORD_CLIENT_SECRET=

########################
# Backend (optional – used for Plus verification, etc.)
########################
BACKEND_API_URL=

########################
# Firebase Web Config (optional; if you use cloud features)
########################
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

########################
# Runtime / Dev settings
########################
# Open DevTools on launch when set to 1
NEBULA_DEVTOOLS=0

# Node environment (defaults to production in packaged app)
NODE_ENV=production

# Security note: Stripe secrets and any server credentials MUST live on your backend –
# never place them in this desktop .env as packaged apps can be inspected.

